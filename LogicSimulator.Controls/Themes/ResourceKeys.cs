using System.Windows;

namespace LogicSimulator.Controls.Themes;

public static class ResourceKeys
{
    public static readonly ComponentResourceKey ButtonBackground = new(typeof(ResourceKeys), nameof(ButtonBackground));
}