<UserControl
    x:Class="LogicSimulator.Views.PropertiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avm="clr-namespace:LogicSimulator.ViewModels.AnchorableViewModels"
    xmlns:b="clr-namespace:LogicSimulator.Infrastructure.Behaviors"
    xmlns:c="clr-namespace:LogicSimulator.Infrastructure.Converters"
    xmlns:cont="clr-namespace:LogicSimulator.Controls;assembly=LogicSimulator.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:inf="clr-namespace:LogicSimulator.Infrastructure"
    xmlns:layout="clr-namespace:LogicSimulator.ViewModels.EditorViewModels.Layout"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=avm:PropertiesViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ScrollViewer
        DataContext="{Binding Path=CurrentEditorViewModel}"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <ScrollViewer.Resources>
            <ResourceDictionary Source="/LogicSimulator.Controls;component/Styles/PropertiesEditorStyles.xaml" />
        </ScrollViewer.Resources>
        <Grid Margin="5" Background="#404040">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                Margin="4,4"
                HorizontalAlignment="Left"
                Text="{Binding Path=LayoutViewModel.ObjectName}" />
            <ItemsControl
                Grid.Row="1"
                Grid.IsSharedSizeScope="True"
                ItemsSource="{Binding Path=LayoutViewModel.Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding Path=Name}">
                            <ItemsControl Margin="3" ItemsSource="{Binding Path=EditorRows}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DataTemplate.Resources>
                                            <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="{x:Type TextBlock}">
                                                <Setter Property="Margin" Value="5" />
                                                <Setter Property="VerticalAlignment" Value="Center" />
                                                <Setter Property="HorizontalAlignment" Value="Right" />
                                            </Style>
                                        </DataTemplate.Resources>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" SharedSizeGroup="A" />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Grid.Column="0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Text="{Binding Path=Name}" />
                                            <ItemsControl
                                                x:Name="Properties"
                                                Grid.Column="1"
                                                ItemsSource="{Binding Path=ObjectProperties}">

                                                <ItemsControl.Resources>
                                                    <DataTemplate x:Key="NumberBox" DataType="{x:Type layout:PropertyViewModel}">
                                                        <cont:NumberBox IsValueUndefined="{Binding Path=IsValueUndefined}" Number="{Binding Path=Value}" />
                                                    </DataTemplate>

                                                    <!--
                                                    <DataTemplate x:Key="NumberBox" DataType="{x:Type layout:ObjectProperty}">
                                                        <cont:NumberBox>
                                                            <i:Interaction.Behaviors>
                                                                <b:PropertyEditorBehavior
                                                                    EditorDependencyProperty="{x:Static cont:NumberBox.NumberProperty}"
                                                                    EditorViewModel="{Binding Path=DataContext,
                                                                                              RelativeSource={RelativeSource AncestorType={x:Type ScrollViewer}}}"
                                                                    ObjectPropertyName="{Binding Path=ObjectPropertyName}">
                                                                    <b:PropertyEditorBehavior.UndefinedProperties>
                                                                        <b:UndefinedProperty DependencyProperty="{x:Static cont:NumberBox.IsValueUndefinedProperty}" />
                                                                    </b:PropertyEditorBehavior.UndefinedProperties>
                                                                </b:PropertyEditorBehavior>
                                                            </i:Interaction.Behaviors>
                                                        </cont:NumberBox>
                                                    </DataTemplate>
                                                    -->
                                                    <DataTemplate x:Key="Vector2Box" DataType="{x:Type layout:PropertyViewModel}">
                                                        <cont:Vector2Box>
                                                            <i:Interaction.Behaviors>
                                                                <b:PropertyEditorBehavior
                                                                    EditorDependencyProperty="{x:Static cont:Vector2Box.VectorProperty}"
                                                                    EditorViewModel="{Binding Path=DataContext,
                                                                                              RelativeSource={RelativeSource AncestorType={x:Type ScrollViewer}}}"
                                                                    ObjectPropertyName="{Binding Path=PropertyName}">
                                                                    <b:PropertyEditorBehavior.UndefinedProperties>
                                                                        <b:UndefinedProperty DependencyProperty="{x:Static cont:Vector2Box.IsVectorXUndefinedProperty}" Suffix="X" />
                                                                        <b:UndefinedProperty DependencyProperty="{x:Static cont:Vector2Box.IsVectorYUndefinedProperty}" Suffix="Y" />
                                                                    </b:PropertyEditorBehavior.UndefinedProperties>
                                                                </b:PropertyEditorBehavior>
                                                            </i:Interaction.Behaviors>
                                                        </cont:Vector2Box>
                                                    </DataTemplate>
                                                    <DataTemplate x:Key="ColorPicker" DataType="{x:Type layout:PropertyViewModel}">
                                                        <cont:ColorPicker>
                                                            <i:Interaction.Behaviors>
                                                                <b:PropertyEditorBehavior
                                                                    EditorDependencyProperty="{x:Static cont:ColorPicker.ColorProperty}"
                                                                    EditorViewModel="{Binding Path=DataContext,
                                                                                              RelativeSource={RelativeSource AncestorType={x:Type ScrollViewer}}}"
                                                                    ObjectPropertyName="{Binding Path=PropertyName}">
                                                                    <b:PropertyEditorBehavior.UndefinedProperties>
                                                                        <b:UndefinedProperty DependencyProperty="{x:Static cont:ColorPicker.IsValueUndefinedProperty}" />
                                                                    </b:PropertyEditorBehavior.UndefinedProperties>
                                                                </b:PropertyEditorBehavior>
                                                            </i:Interaction.Behaviors>
                                                        </cont:ColorPicker>
                                                    </DataTemplate>
                                                    <DataTemplate x:Key="CheckBoxEx" DataType="{x:Type layout:PropertyViewModel}">
                                                        <cont:CheckBoxEx>
                                                            <i:Interaction.Behaviors>
                                                                <b:PropertyEditorBehavior
                                                                    EditorDependencyProperty="{x:Static cont:CheckBoxEx.IsCheckedProperty}"
                                                                    EditorViewModel="{Binding Path=DataContext,
                                                                                              RelativeSource={RelativeSource AncestorType={x:Type ScrollViewer}}}"
                                                                    ObjectPropertyName="{Binding Path=PropertyName}">
                                                                    <b:PropertyEditorBehavior.UndefinedProperties>
                                                                        <b:UndefinedProperty DependencyProperty="{x:Static cont:CheckBoxEx.IsValueUndefinedProperty}" />
                                                                    </b:PropertyEditorBehavior.UndefinedProperties>
                                                                </b:PropertyEditorBehavior>
                                                            </i:Interaction.Behaviors>
                                                        </cont:CheckBoxEx>
                                                    </DataTemplate>
                                                </ItemsControl.Resources>
                                                <ItemsControl.ItemTemplateSelector>
                                                    <inf:EditorPropertiesDataTemplateSelector />
                                                </ItemsControl.ItemTemplateSelector>
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <cont:GridEx ColumnsLayout="{Binding Path=Layout}" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Path=Name}" Value="">
                                                <Setter TargetName="Properties" Property="Grid.ColumnSpan" Value="2" />
                                                <Setter TargetName="Properties" Property="Grid.Column" Value="0" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
