<UserControl
    x:Class="LogicSimulator.Views.PropertiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avm="clr-namespace:LogicSimulator.ViewModels.AnchorableViewModels"
    xmlns:base="clr-namespace:LogicSimulator.ViewModels.EditorViewModels.Base"
    xmlns:cont="clr-namespace:LogicSimulator.Controls;assembly=LogicSimulator.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dx="clr-namespace:SharpDX;assembly=SharpDX.Mathematics"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
    d:DataContext="{d:DesignInstance Type=avm:PropertiesViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ScrollViewer
        DataContext="{Binding Path=CurrentEditorViewModel}"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <ScrollViewer.Resources>
            <ResourceDictionary Source="/LogicSimulator.Controls;component/Styles/PropertiesEditorStyles.xaml" />
        </ScrollViewer.Resources>
        <Grid Margin="5" Background="#404040">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                Margin="4,4"
                HorizontalAlignment="Left"
                Text="{Binding Path=Layout.ObjectName}" />
            <ItemsControl
                Grid.Row="1"
                Grid.IsSharedSizeScope="True"
                ItemsSource="{Binding Path=Layout.Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding Path=Name}">
                            <ItemsControl Margin="3" ItemsSource="{Binding Path=EditorRows}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DataTemplate.Resources>
                                            <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="{x:Type TextBlock}">
                                                <Setter Property="Margin" Value="5" />
                                                <Setter Property="VerticalAlignment" Value="Center" />
                                                <Setter Property="HorizontalAlignment" Value="Right" />
                                            </Style>
                                        </DataTemplate.Resources>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" SharedSizeGroup="A" />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Grid.Column="0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Text="{Binding Path=Name}" />
                                            <ItemsControl
                                                x:Name="Properties"
                                                Grid.Column="1"
                                                ItemsSource="{Binding Path=ObjectProperties}">

                                                <ItemsControl.ItemContainerStyle>
                                                    <Style>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Path=PropertyPropertyType}" Value="{x:Type sys:Single}">
                                                                <DataTrigger.Setters>
                                                                    <Setter Property="ContentControl.ContentTemplate" Value="{DynamicResource FloatPropertyTemplate}" />
                                                                </DataTrigger.Setters>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Path=PropertyPropertyType}" Value="{x:Type dx:Vector2}">
                                                                <DataTrigger.Setters>
                                                                    <Setter Property="ContentControl.ContentTemplate" Value="{DynamicResource Vector2PropertyTemplate}" />
                                                                </DataTrigger.Setters>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Path=PropertyPropertyType}" Value="{x:Type dx:Color4}">
                                                                <DataTrigger.Setters>
                                                                    <Setter Property="ContentControl.ContentTemplate" Value="{DynamicResource Color4PropertyTemplate}" />
                                                                </DataTrigger.Setters>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Path=PropertyPropertyType}" Value="{x:Type sys:Boolean}">
                                                                <DataTrigger.Setters>
                                                                    <Setter Property="ContentControl.ContentTemplate" Value="{DynamicResource BoolPropertyTemplate}" />
                                                                </DataTrigger.Setters>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                        <Setter Property="ContentControl.ContentTemplate" Value="{DynamicResource ErrorPropertyTemplate}" />
                                                    </Style>
                                                </ItemsControl.ItemContainerStyle>
                                                <ItemsControl.Resources>
                                                    <DataTemplate x:Key="FloatPropertyTemplate" DataType="{x:Type base:PropertyViewModel}">
                                                        <cont:NumberBox IsValueUndefined="{Binding Path=IsValueUndefined}" Number="{Binding Path=Value}" />
                                                    </DataTemplate>
                                                    <DataTemplate x:Key="Vector2PropertyTemplate" DataType="{x:Type base:PropertyViewModel}">
                                                        <cont:Vector2Box
                                                            IsVectorXUndefined="{Binding Path=IsXUndefined}"
                                                            IsVectorYUndefined="{Binding Path=IsYUndefined}"
                                                            Vector="{Binding Path=Value}" />
                                                    </DataTemplate>
                                                    <DataTemplate x:Key="Color4PropertyTemplate" DataType="{x:Type base:PropertyViewModel}">
                                                        <cont:ColorPicker IsValueUndefined="{Binding Path=IsValueUndefined}" Color="{Binding Path=Value}" />
                                                    </DataTemplate>
                                                    <DataTemplate x:Key="BoolPropertyTemplate" DataType="{x:Type base:PropertyViewModel}">
                                                        <cont:CheckBoxEx IsChecked="{Binding Path=Value}" IsValueUndefined="{Binding Path=IsValueUndefined}" />
                                                    </DataTemplate>
                                                    <DataTemplate x:Key="ErrorPropertyTemplate">
                                                        <cont:TextBoxEx
                                                            VerticalAlignment="Center"
                                                            IsHasError="True"
                                                            IsReadOnly="True"
                                                            Text="Шаблон не найден!"
                                                            ToolTip="{Binding}" />
                                                    </DataTemplate>
                                                </ItemsControl.Resources>
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <cont:GridEx ColumnsLayout="{Binding Path=Layout}" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Path=Name}" Value="">
                                                <Setter TargetName="Properties" Property="Grid.ColumnSpan" Value="2" />
                                                <Setter TargetName="Properties" Property="Grid.Column" Value="0" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
