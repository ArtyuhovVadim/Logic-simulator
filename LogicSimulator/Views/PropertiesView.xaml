<UserControl
    x:Class="LogicSimulator.Views.PropertiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:editorViews="clr-namespace:LogicSimulator.Views.EditorViews"
    xmlns:evm="clr-namespace:LogicSimulator.ViewModels.EditorViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:LogicSimulator.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PropertiesViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type evm:RectangleEditorViewModel}">
            <editorViews:RectangleEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type evm:RoundedRectangleEditorViewModel}">
            <editorViews:RoundedRectangleEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type evm:EllipseEditorViewModel}">
            <editorViews:EllipseEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type evm:LineEditorViewModel}">
            <editorViews:LineEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type evm:BezierCurveViewModel}">
            <editorViews:BezierCurveEditorView />
        </DataTemplate>
    </UserControl.Resources>
    <ContentPresenter Content="{Binding Path=CurrentEditorViewModel}" />
</UserControl>
