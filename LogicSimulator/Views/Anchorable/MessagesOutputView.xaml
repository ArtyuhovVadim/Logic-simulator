<UserControl
    x:Class="LogicSimulator.Views.Anchorable.MessagesOutputView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behaviors="clr-namespace:LogicSimulator.Infrastructure.Behaviors"
    xmlns:brushes="clr-namespace:WpfExtensions.Controls.Resources.Brushes;assembly=WpfExtensions.Controls"
    xmlns:commands="clr-namespace:LogicSimulator.Infrastructure.Commands"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:logicSimulator="clr-namespace:LogicSimulator"
    xmlns:lsbk="clr-namespace:LogicSimulator.Resources.Brushes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:avm="clr-namespace:LogicSimulator.ViewModels.Anchorable"
    xmlns:common="clr-namespace:LogicSimulator.ViewModels.Common"
    d:DataContext="{d:DesignInstance Type=avm:MessagesOutputViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <DockPanel Margin="6">
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="0.5*" />
                <ColumnDefinition Width="*" MaxWidth="200" />
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <!--  Override default ToggleButton brush  -->
                <SolidColorBrush x:Key="{x:Static brushes:BrushesKeys+ToggleButton.CheckedBackground}" Color="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.ToggleButtonCheckedColor}}" />
                <Style BasedOn="{StaticResource {x:Type ToggleButton}}" TargetType="{x:Type ToggleButton}">
                    <Setter Property="Padding" Value="4,2" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static brushes:BrushesKeys+ToggleButton.BorderBrush}}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="{x:Type Path}">
                    <Setter Property="Width" Value="13" />
                    <Setter Property="Height" Value="13" />
                    <Setter Property="Stretch" Value="Uniform" />
                    <Setter Property="UseLayoutRounding" Value="True" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                </Style>
            </Grid.Resources>
            <StackPanelEx
                Grid.Column="0"
                Gap="3"
                Orientation="Horizontal">
                <ToggleButton IsChecked="{Binding Path=IsErrorMessagesVisible}">
                    <ToggleButton.Content>
                        <StackPanelEx Gap="5" Orientation="Horizontal">
                            <Grid>
                                <Rectangle Margin="3" Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.BackgroundIconBrush}}" />
                                <Path Data="{StaticResource ErrorIcon}" Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.ErrorIconBrush}}" />
                            </Grid>
                            <TextBlock Text="Ошибки" />
                        </StackPanelEx>
                    </ToggleButton.Content>
                </ToggleButton>
                <Separator Style="{StaticResource VerticalSeparator}" />
                <ToggleButton IsChecked="{Binding Path=IsWarningMessagesVisible}">
                    <ToggleButton.Content>
                        <StackPanelEx Gap="5" Orientation="Horizontal">
                            <Grid>
                                <Rectangle Margin="5,2,5,2" Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.BackgroundIconBrush}}" />
                                <Path Data="{StaticResource ExclamationTriangleIcon}" Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.WarningIconBrush}}" />
                            </Grid>
                            <TextBlock Text="Предупреждения" />
                        </StackPanelEx>
                    </ToggleButton.Content>
                </ToggleButton>
                <Separator Style="{StaticResource VerticalSeparator}" />
                <ToggleButton IsChecked="{Binding Path=IsInformationMessagesVisible}">
                    <ToggleButton.Content>
                        <StackPanelEx Gap="5" Orientation="Horizontal">
                            <Grid>
                                <Rectangle Margin="5,2,5,2" Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.BackgroundIconBrush}}" />
                                <Path Data="{StaticResource InfoCircleIcon}" Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.InformationIconBrush}}" />
                            </Grid>
                            <TextBlock Text="Сообщения" />
                        </StackPanelEx>
                    </ToggleButton.Content>
                </ToggleButton>
                <Separator Style="{StaticResource VerticalSeparator}" />
                <If Condition="{Binding Path=., Source={x:Static logicSimulator:App.IsDevelopment}}">
                    <If.True>
                        <ToggleButton IsChecked="{Binding Path=IsDebugMessagesVisible}">
                            <ToggleButton.Content>
                                <StackPanelEx Gap="5" Orientation="Horizontal">
                                    <Path Data="{StaticResource DebugIcon}" Fill="#EDAE27" />
                                    <TextBlock Text="Отладка" />
                                    <TextBlock Text="" />
                                </StackPanelEx>
                            </ToggleButton.Content>
                        </ToggleButton>
                    </If.True>
                </If>
                <If Condition="{Binding Path=., Source={x:Static logicSimulator:App.IsDevelopment}}">
                    <If.True>
                        <Separator Style="{StaticResource VerticalSeparator}" />
                    </If.True>
                </If>
                <Button
                    Width="{Binding RelativeSource={RelativeSource Self},
                                    Path=ActualHeight}"
                    Padding="2"
                    Command="{Binding Path=ClearFiltersCommand}">
                    <Button.Content>
                        <Path
                            Width="15"
                            Height="15"
                            Data="{StaticResource ClearFilterIcon}"
                            Stroke="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.ClearFilterIconBrush}}" />
                    </Button.Content>
                </Button>
                <Separator Style="{StaticResource VerticalSeparator}" />
                <Button
                    Width="{Binding RelativeSource={RelativeSource Self},
                                    Path=ActualHeight}"
                    Padding="2"
                    Command="{Binding Path=ClearMessagesCommand}">
                    <Button.Content>
                        <Path
                            Width="15"
                            Height="15"
                            Data="{StaticResource ClearAllIcon}"
                            Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.ClearFilterIconBrush}}" />
                        <!--  TODO: Icon brush ^^^  -->
                    </Button.Content>
                </Button>
            </StackPanelEx>
            <TextBoxEx
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                IconMargin="3 0 0 0"
                SelectAllTextOnFocus="True"
                Text="{Binding Path=SearchText}"
                Watermark="Поиск">
                <TextBoxEx.Icon>
                    <Path Data="{StaticResource SearchIcon}" Stroke="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.SearchIconBrush}}" />
                </TextBoxEx.Icon>
            </TextBoxEx>
        </Grid>
        <DataGrid
            Margin="0,6,0,0"
            AutoGenerateColumns="False"
            BorderThickness="1"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserReorderColumns="False"
            HeadersVisibility="Column"
            ItemsSource="{Binding Path=Messages}"
            SelectionMode="Single"
            SelectionUnit="FullRow">

            <DataGrid.Resources>
                <commands:CopyToClipboardCommand x:Key="CopyToClipboardCommand" />

                <ContextMenu x:Key="DataGridRowContextMenu" d:DataContext="{d:DesignInstance Type=common:OutputMessageViewModel}">
                    <MenuItem
                        Command="{StaticResource CopyToClipboardCommand}"
                        CommandParameter="{Binding Path=Text}"
                        Header="Копировать сообщение"
                        InputGestureText="Ctrl+C" />
                    <MenuItem
                        Command="{Binding Path=DataContext.GoToMessageSourceCommand,
                                          RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                        CommandParameter="{Binding Path=Source}"
                        Header="Перейти к источнику" />
                </ContextMenu>
            </DataGrid.Resources>

            <DataGrid.InputBindings>
                <KeyBinding
                    Key="C"
                    Command="{StaticResource CopyToClipboardCommand}"
                    CommandParameter="{Binding Path=SelectedItem.Text,
                                               RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                    Modifiers="Control" />
            </DataGrid.InputBindings>

            <b:Interaction.Behaviors>
                <behaviors:DataGridScrollToLastItemBehaviour />
            </b:Interaction.Behaviors>

            <DataGrid.RowStyle>
                <Style BasedOn="{StaticResource {x:Type DataGridRow}}" TargetType="{x:Type DataGridRow}">
                    <Setter Property="ContextMenu" Value="{StaticResource DataGridRowContextMenu}" />
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTemplateColumn
                    Width="Auto"
                    CanUserResize="False"
                    CanUserSort="True"
                    IsReadOnly="True"
                    SortMemberPath="Type">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type common:OutputMessageViewModel}">
                            <Grid Margin="3">
                                <Ellipse
                                    x:Name="Ellipse"
                                    Width="10"
                                    Height="10"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Fill="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.BackgroundIconBrush}}" />
                                <Path
                                    x:Name="Path"
                                    Width="13"
                                    Height="13"
                                    SnapsToDevicePixels="True"
                                    Stretch="Uniform"
                                    UseLayoutRounding="True" />
                            </Grid>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Path=Type}" Value="Information">
                                    <Setter TargetName="Path" Property="Data" Value="{StaticResource InfoCircleIcon}" />
                                    <Setter TargetName="Path" Property="Fill" Value="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.InformationIconBrush}}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Type}" Value="Warning">
                                    <Setter TargetName="Path" Property="Data" Value="{StaticResource ExclamationTriangleIcon}" />
                                    <Setter TargetName="Path" Property="Fill" Value="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.WarningIconBrush}}" />
                                    <Setter TargetName="Ellipse" Property="Margin" Value="4,3,4,2" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Type}" Value="Error">
                                    <Setter TargetName="Path" Property="Data" Value="{StaticResource ErrorIcon}" />
                                    <Setter TargetName="Path" Property="Fill" Value="{DynamicResource {x:Static lsbk:BrushesKeys+MessagesOutputView.ErrorIconBrush}}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Type}" Value="Debug">
                                    <Setter TargetName="Path" Property="Data" Value="{StaticResource DebugIcon}" />
                                    <Setter TargetName="Path" Property="Fill" Value="#EDAE27" />
                                    <Setter TargetName="Ellipse" Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn
                    Width="Auto"
                    CanUserSort="True"
                    Header="Время"
                    IsReadOnly="True"
                    SortMemberPath="Time">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type common:OutputMessageViewModel}">
                            <TextBlock
                                Margin="3,0,3,0"
                                VerticalAlignment="Center"
                                Text="{Binding Path=Time,
                                               StringFormat={}{0:HH:mm:ss}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn
                    Width="*"
                    CanUserSort="True"
                    Header="Сообщение"
                    IsReadOnly="False"
                    SortMemberPath="Text">
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate DataType="{x:Type common:OutputMessageViewModel}">
                            <TextBoxEx
                                Padding="3,0,0,0"
                                VerticalAlignment="Stretch"
                                BorderThickness="0"
                                CornerRadius="0"
                                IsReadOnly="True"
                                Text="{Binding Path=Text}"
                                TextWrapping="WrapWithOverflow" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type common:OutputMessageViewModel}">
                            <TextBlock
                                MaxHeight="128"
                                Margin="3,0,0,0"
                                VerticalAlignment="Center"
                                Text="{Binding Path=Text}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="WrapWithOverflow" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn
                    Width="Auto"
                    CanUserSort="True"
                    Header="Источник"
                    IsReadOnly="True"
                    SortMemberPath="Source">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type common:OutputMessageViewModel}">
                            <Grid Background="Transparent">
                                <Grid.InputBindings>
                                    <MouseBinding
                                        Command="{Binding Path=DataContext.GoToMessageSourceCommand,
                                                          RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                        CommandParameter="{Binding Path=Source}"
                                        MouseAction="LeftDoubleClick" />
                                </Grid.InputBindings>
                                <TextBlock
                                    Margin="3,0,3,0"
                                    VerticalAlignment="Center"
                                    Text="{Binding Path=Source.Name}"
                                    TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
